# Agent guidance

All detailed conventions and LiveKit workflow guidance for this repo live in **`.cursor/rules/*.mdc`**. Cursor loads those rules automatically; other agents should read them directly (the body is Markdown after the YAML front matter).

Human-oriented setup and feature list: **`README.md`**.
